```
# /etc/hostapd.conf

# 1. 接口和驱动
interface=wlan0
driver=nl80211

# 2. AP 热点名称 (SSID)
ssid=Echo-mate-Setup

# 3. 硬件模式和信道 (g = 2.4GHz)
hw_mode=g
channel=6

# 4. WPA2 安全设置 (最关键的部分)
#    (WPA3需要 hostapd v2.8+ 和特定驱动支持，WPA2 兼容性最好)
wpa=2
wpa_key_mgmt=WPA-PSK
wpa_passphrase=YourPassword # 设置一个用于配网的临时密码，例如 12345678
rsn_pairwise=CCMP
```